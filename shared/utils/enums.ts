export const projectMembershipTypes = Object.freeze({
  ADMIN: 'admin',
  MEMBER: 'member',
  GUEST: 'guest'
})

export const workItemTypes = Object.freeze({
  TAKS: 'task',
  BUG: 'bug'
})

export const workItemStatus = Object.freeze({
  TOOD: 'TODO',
  IN_PROGRESS: 'In Progress',
  COMPLETED: 'Completed',
  CANCELLED: 'Cancelled',
  BLOCKED: 'Blocked'
})

export const prefKeys = Object.freeze({
  LAST_VISITED_PROJECT_ID: 'lastVisitedProjectId'
})
